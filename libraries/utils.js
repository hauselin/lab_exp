/**
 * utils.js
 **/

function range(start, end) {
  var ans = [];
  for (let i = start; i <= end; i++) {
    ans.push(i);
  }
  return ans;
}

function iti_exponential(low, high) {
  // do something
  return undefined;
}

function random_choice(array) {
  // randomly select an item from array
  return undefined;
}